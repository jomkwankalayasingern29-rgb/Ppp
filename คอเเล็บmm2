-- ==================== GKHub X PancakeHub & MM2 UI ====================
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

if CoreGui:FindFirstChild("DeltaCustomUI") then 
    CoreGui.DeltaCustomUI:Destroy() 
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DeltaCustomUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

-- **Toggle Button (Text Style)**
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleIcon"
ToggleBtn.Size = UDim2.new(0, 44, 0, 44)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.2, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
ToggleBtn.Text = "G"
ToggleBtn.TextColor3 = Color3.fromRGB(0, 162, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 16
ToggleBtn.Active = true
ToggleBtn.Draggable = true
ToggleBtn.Parent = ScreenGui

Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 10)
local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Color = Color3.fromRGB(0, 162, 255)
ToggleStroke.Thickness = 1.5

-- **Loading Screen**
local LoadFrame = Instance.new("Frame")
LoadFrame.Name = "LoadFrame"
LoadFrame.Size = UDim2.new(0, 300, 0, 65)
LoadFrame.Position = UDim2.new(0.5, -150, 0.5, -32)
LoadFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LoadFrame.BorderSizePixel = 0
LoadFrame.Parent = ScreenGui

Instance.new("UICorner", LoadFrame).CornerRadius = UDim.new(0, 8)
local LoadStroke = Instance.new("UIStroke", LoadFrame)
LoadStroke.Color = Color3.fromRGB(0, 162, 255)
LoadStroke.Thickness = 1.5

local LoadText = Instance.new("TextLabel")
LoadText.Name = "LoadText"
LoadText.Size = UDim2.new(1, 0, 1, 0)
LoadText.BackgroundTransparency = 1
LoadText.Font = Enum.Font.GothamBold
LoadText.Text = "กำลังโหลด..."
LoadText.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadText.TextSize = 16
LoadText.Parent = LoadFrame

-- **Main Variables**
local baseWidth = 520
local baseHeight = 340
local currentScale = 1 
local currentAlpha = 1 
local textLabelData = {}

-- **Main Window Frame**
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, baseWidth, 0, baseHeight)
MainFrame.Position = UDim2.new(0.5, -baseWidth/2, 0.5, -baseHeight/2)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
MainFrame.ClipsDescendants = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)
MainFrame.BackgroundTransparency = 1

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(0, 162, 255)
MainStroke.Thickness = 1.5

-- **TopBar**
local TopBar = Instance.new("Frame", MainFrame)
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
TopBar.BorderSizePixel = 0
TopBar.BackgroundTransparency = 1
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 8)

-- Fix TopBar bottom sharp corners
local FixTopBar = Instance.new("Frame", TopBar)
FixTopBar.Size = UDim2.new(1, 0, 0, 5)
FixTopBar.Position = UDim2.new(0, 0, 1, -5)
FixTopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
FixTopBar.BorderSizePixel = 0

-- Logo G (No Emoji)
local LogoG = Instance.new("TextLabel", TopBar)
LogoG.Size = UDim2.new(0, 24, 0, 24)
LogoG.Position = UDim2.new(0, 10, 0, 5.5)
LogoG.BackgroundTransparency = 1
LogoG.Font = Enum.Font.GothamBold
LogoG.Text = "G"
LogoG.TextColor3 = Color3.fromRGB(0, 162, 255)
LogoG.TextSize = 14

local TitleText = Instance.new("TextLabel", TopBar)
TitleText.Size = UDim2.new(0, 380, 1, 0)
TitleText.Position = UDim2.new(0, 38, 0, 0)
TitleText.Text = "ค่ายGKHub X ค่ายPancakeHub & MM2"
TitleText.TextColor3 = Color3.fromRGB(240, 240, 240)
TitleText.Font = Enum.Font.GothamBold
TitleText.TextSize = 12
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.BackgroundTransparency = 1
table.insert(textLabelData, {Obj = TitleText, BaseSize = 12})

local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -33, 0, 3.5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(220, 60, 60)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 13
table.insert(textLabelData, {Obj = CloseBtn, BaseSize = 13})

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

local MinBtn = Instance.new("TextButton", TopBar)
MinBtn.Size = UDim2.new(0, 28, 0, 28)
MinBtn.Position = UDim2.new(1, -61, 0, 3.5)
MinBtn.BackgroundTransparency = 1
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 14
table.insert(textLabelData, {Obj = MinBtn, BaseSize = 14})

-- **Sidebar & Content Area**
local Sidebar = Instance.new("ScrollingFrame", MainFrame)
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0.28, 0, 1, -43)
Sidebar.Position = UDim2.new(0, 6, 0, 38)
Sidebar.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
Sidebar.BorderSizePixel = 0
Sidebar.ScrollBarThickness = 2
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
Sidebar.AutomaticCanvasSize = Enum.AutomaticSize.Y
Sidebar.BackgroundTransparency = 1
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 6)

local SideLayout = Instance.new("UIListLayout", Sidebar)
SideLayout.SortOrder = Enum.SortOrder.LayoutOrder
SideLayout.Padding = UDim.new(0, 5)
SideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local ContentArea = Instance.new("Frame", MainFrame)
ContentArea.Name = "ContentArea"
ContentArea.Size = UDim2.new(0.69, 0, 1, -43)
ContentArea.Position = UDim2.new(0.30, 0, 0, 38)
ContentArea.BackgroundTransparency = 1

local tabs = {}

local function CreateTab(name)
    local TabBtn = Instance.new("TextButton", Sidebar)
    TabBtn.Size = UDim2.new(0.95, 0, 0, 30)
    TabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
    TabBtn.Text = name
    TabBtn.TextColor3 = Color3.fromRGB(180, 180, 190)
    TabBtn.Font = Enum.Font.GothamMedium
    TabBtn.TextSize = 11
    Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 4)
    table.insert(textLabelData, {Obj = TabBtn, BaseSize = 11})

    local Page = Instance.new("ScrollingFrame", ContentArea)
    Page.Name = name .. "Page"
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 2
    Page.CanvasSize = UDim2.new(0, 0, 0, 0)
    Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Page.BorderSizePixel = 0

    local PageLayout = Instance.new("UIListLayout", Page)
    PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    PageLayout.Padding = UDim.new(0, 6)
    PageLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

    tabs[name] = {Btn = TabBtn, Page = Page}

    TabBtn.MouseButton1Click:Connect(function()
        for _, t in pairs(tabs) do
            t.Page.Visible = false
            t.Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
            t.Btn.TextColor3 = Color3.fromRGB(180, 180, 190)
        end
        Page.Visible = true
        TabBtn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
        TabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)

    return Page
end

local MainInfoPage = CreateTab("หลัก")
local AttackPage = CreateTab("โจมตี")
local HelperPage = CreateTab("ช่วยเล่น")
local UIPage = CreateTab("UI")
local PlayerPage = CreateTab("ตัวละคร")
local ToolPage = CreateTab("เครื่องมือ")
local ESPPage = CreateTab("ESP")

tabs["หลัก"].Page.Visible = true
tabs["หลัก"].Btn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
tabs["หลัก"].Btn.TextColor3 = Color3.fromRGB(255, 255, 255)

local function CreateSlider(parent, titleText, minVal, maxVal, defaultVal, suffix, callback)
    local SliderFrame = Instance.new("Frame", parent)
    SliderFrame.Size = UDim2.new(0.96, 0, 0, 46)
    SliderFrame.BackgroundColor3 = Color3.fromRGB(26, 26, 32)
    Instance.new("UICorner", SliderFrame).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel", SliderFrame)
    Label.Size = UDim2.new(0.6, 0, 0, 20)
    Label.Position = UDim2.new(0, 8, 0, 4)
    Label.Text = titleText
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    table.insert(textLabelData, {Obj = Label, BaseSize = 11})

    local ValueLabel = Instance.new("TextLabel", SliderFrame)
    ValueLabel.Size = UDim2.new(0.3, 0, 0, 20)
    ValueLabel.Position = UDim2.new(0.67, 0, 0, 4)
    ValueLabel.Text = tostring(defaultVal) .. suffix
    ValueLabel.TextColor3 = Color3.fromRGB(0, 162, 255)
    ValueLabel.Font = Enum.Font.GothamBold
    ValueLabel.TextSize = 11
    ValueLabel.TextXAlignment = Enum.TextXAlignment.Right
    ValueLabel.BackgroundTransparency = 1
    table.insert(textLabelData, {Obj = ValueLabel, BaseSize = 11})

    local SliderBar = Instance.new("Frame", SliderFrame)
    SliderBar.Size = UDim2.new(1, -16, 0, 6)
    SliderBar.Position = UDim2.new(0, 8, 0, 28)
    SliderBar.BackgroundColor3 = Color3.fromRGB(38, 38, 48)
    SliderBar.BorderSizePixel = 0
    Instance.new("UICorner", SliderBar).CornerRadius = UDim.new(1, 0)

    local SliderFill = Instance.new("Frame", SliderBar)
    SliderFill.Size = UDim2.new((defaultVal - minVal)/(maxVal - minVal), 0, 1, 0)
    SliderFill.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
    SliderFill.BorderSizePixel = 0
    Instance.new("UICorner", SliderFill).CornerRadius = UDim.new(1, 0)

    local isDragging = false
    local function updateSlider(input)
        local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
        local val = math.floor(minVal + (maxVal - minVal) * pos)
        SliderFill.Size = UDim2.new(pos, 0, 1, 0)
        ValueLabel.Text = tostring(val) .. suffix
        callback(val)
    end

    SliderBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
            updateSlider(input)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            isDragging = false
        end
    end)
end

local function CreateCreditCard(parent, hubName, discordLink, themeColor)
    local Card = Instance.new("Frame", parent)
    Card.Size = UDim2.new(0.96, 0, 0, 65)
    Card.BackgroundColor3 = Color3.fromRGB(26, 26, 32)
    Instance.new("UICorner", Card).CornerRadius = UDim.new(0, 6)

    local Stroke = Instance.new("UIStroke", Card)
    Stroke.Color = themeColor
    Stroke.Thickness = 1

    local Title = Instance.new("TextLabel", Card)
    Title.Size = UDim2.new(1, -12, 0, 20)
    Title.Position = UDim2.new(0, 8, 0, 6)
    Title.Text = "เครดิตผู้สร้าง: " .. hubName
    Title.TextColor3 = themeColor
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 11
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.BackgroundTransparency = 1
    table.insert(textLabelData, {Obj = Title, BaseSize = 11})

    local CopyBtn = Instance.new("TextButton", Card)
    CopyBtn.Size = UDim2.new(1, -16, 0, 26)
    CopyBtn.Position = UDim2.new(0, 8, 0, 31)
    CopyBtn.BackgroundColor3 = themeColor
    CopyBtn.Text = "คัดลอกลิงก์ Discord"
    CopyBtn.TextColor3 = Color3.fromRGB(20, 20, 24)
    CopyBtn.Font = Enum.Font.GothamBold
    CopyBtn.TextSize = 10
    Instance.new("UICorner", CopyBtn).CornerRadius = UDim.new(0, 4)
    table.insert(textLabelData, {Obj = CopyBtn, BaseSize = 10})

    CopyBtn.MouseButton1Click:Connect(function()
        pcall(function() setclipboard(discordLink) end)
        CopyBtn.Text = "คัดลอกสำเร็จ!"
        task.wait(1.5)
        CopyBtn.Text = "คัดลอกลิงก์ Discord"
    end)
end

CreateCreditCard(MainInfoPage, "PancakeHub", "https://discord.gg/HfnJnAAM8e", Color3.fromRGB(0, 162, 255))
CreateCreditCard(MainInfoPage, "GKHub", "https://discord.gg/Ugssja4wqQ", Color3.fromRGB(80, 150, 220))

local function CreateFeatureRow(parent, labelText, buttonText, buttonColor, callback)
    local Frame = Instance.new("Frame", parent)
    Frame.Size = UDim2.new(0.96, 0, 0, 38)
    Frame.BackgroundColor3 = Color3.fromRGB(26, 26, 32)
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel", Frame)
    Label.Size = UDim2.new(0.6, 0, 1, 0)
    Label.Position = UDim2.new(0, 8, 0, 0)
    Label.Text = labelText
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    table.insert(textLabelData, {Obj = Label, BaseSize = 11})

    local Btn = Instance.new("TextButton", Frame)
    Btn.Size = UDim2.new(0, 75, 0, 24)
    Btn.Position = UDim2.new(1, -83, 0.5, -12)
    Btn.Font = Enum.Font.GothamBold
    Btn.TextSize = 10
    Btn.Text = buttonText
    Btn.BackgroundColor3 = buttonColor
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 4)
    table.insert(textLabelData, {Obj = Btn, BaseSize = 10})

    Btn.MouseButton1Click:Connect(function()
        callback(Btn)
    end)
    return Frame, Btn
end

-- **Attack Module**
local expandHitboxEnabled = false
CreateFeatureRow(AttackPage, "ฆ่าทั้งแมพ (Hitbox)", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    expandHitboxEnabled = not expandHitboxEnabled
    btn.Text = expandHitboxEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = expandHitboxEnabled and Color3.fromRGB(220, 80, 80) or Color3.fromRGB(50, 50, 60)
    
    if not expandHitboxEnabled then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local root = player.Character:FindFirstChild("HumanoidRootPart")
                if root then
                    root.Size = Vector3.new(2, 2, 1)
                    root.Transparency = 1
                end
            end
        end
    end
end)

RunService.Stepped:Connect(function()
    if expandHitboxEnabled then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
                if otherRoot then
                    otherRoot.Size = Vector3.new(999, 999, 999)
                    otherRoot.Transparency = 1
                    otherRoot.CanCollide = false
                end
            end
        end
    end
end)

-- **Helper Module**
local autoFarmCoinsEnabled = false
CreateFeatureRow(HelperPage, "ออโต้เก็บเหรียญ (Zero Delay)", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    autoFarmCoinsEnabled = not autoFarmCoinsEnabled
    btn.Text = autoFarmCoinsEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = autoFarmCoinsEnabled and Color3.fromRGB(0, 162, 255) or Color3.fromRGB(50, 50, 60)
end)

local function GetInstantCoin()
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("BasePart") then
            local name = string.lower(obj.Name)
            if name == "coin" or name == "cd" or obj.Parent.Name == "CoinContainer" or obj:FindFirstChild("Coin") then
                return obj
            end
        end
    end
    return nil
end

RunService.RenderStepped:Connect(function()
    if autoFarmCoinsEnabled then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local coinPart = GetInstantCoin()
            if coinPart then
                char.HumanoidRootPart.CFrame = coinPart.CFrame
            end
        end
    end
end)

CreateFeatureRow(HelperPage, "วาร์ปไปเก็บเหรียญทันที", "วาร์ป", Color3.fromRGB(0, 162, 255), function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local coinPart = GetInstantCoin()
        if coinPart then
            char.HumanoidRootPart.CFrame = coinPart.CFrame
        end
    end
end)

-- **UI Customization Module**
CreateSlider(UIPage, "ความทึบของ UI", 1, 100, 100, "%", function(val)
    currentAlpha = val / 100
    local transparency = 1 - currentAlpha
    MainFrame.BackgroundTransparency = transparency
    TopBar.BackgroundTransparency = transparency
    Sidebar.BackgroundTransparency = transparency
end)

CreateSlider(UIPage, "ขนาดความใหญ่ UI", 50, 150, 100, "%", function(val)
    currentScale = val / 100
    local newW = baseWidth * currentScale
    local newH = baseHeight * currentScale
    MainFrame.Size = UDim2.new(0, newW, 0, newH)
    MainFrame.Position = UDim2.new(0.5, -newW/2, 0.5, -newH/2)

    for _, item in ipairs(textLabelData) do
        if item.Obj and item.Obj.Parent then
            item.Obj.TextSize = math.clamp(math.floor(item.BaseSize * currentScale), 8, 26)
        end
    end
end)

-- **Player Module**
CreateSlider(PlayerPage, "ความเร็วในการวิ่ง (WalkSpeed)", 16, 250, 16, "", function(val)
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.WalkSpeed = val
    end
end)

CreateSlider(PlayerPage, "ความสูงในการกระโดด (JumpPower)", 50, 300, 50, "", function(val)
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid.UseJumpPower = true
        char.Humanoid.JumpPower = val
    end
end)

local noclipEnabled = false
CreateFeatureRow(PlayerPage, "เปิดทะลุกำแพง (Noclip)", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    noclipEnabled = not noclipEnabled
    btn.Text = noclipEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(0, 162, 255) or Color3.fromRGB(50, 50, 60)
end)

RunService.Stepped:Connect(function()
    if noclipEnabled then
        local char = LocalPlayer.Character
        if char then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end
end)

local infJumpEnabled = false
CreateFeatureRow(PlayerPage, "กระโดดไม่จำกัด (Inf Jump)", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    infJumpEnabled = not infJumpEnabled
    btn.Text = infJumpEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = infJumpEnabled and Color3.fromRGB(0, 162, 255) or Color3.fromRGB(50, 50, 60)
end)

UserInputService.JumpRequest:Connect(function()
    if infJumpEnabled then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

-- **Tools Module**
CreateFeatureRow(ToolPage, "รับไอเทมคลิกวาร์ป (Click TP)", "รับไอเทม", Color3.fromRGB(0, 162, 255), function()
    local mouse = LocalPlayer:GetMouse()
    local tool = Instance.new("Tool")
    tool.Name = "Click TP Tool"
    tool.RequiresHandle = false
    tool.Parent = LocalPlayer.Backpack

    tool.Activated:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local targetPos = mouse.Hit.Position + Vector3.new(0, 3, 0)
            char.HumanoidRootPart.CFrame = CFrame.new(targetPos)
        end
    end)
end)

-- **ESP Module**
local espEnabled = false
CreateFeatureRow(ESPPage, "เปิดใช้งาน ESP ไฮไลท์ตัวละคร", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    espEnabled = not espEnabled
    btn.Text = espEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = espEnabled and Color3.fromRGB(0, 162, 255) or Color3.fromRGB(50, 50, 60)
end)

local gunEspEnabled = false
CreateFeatureRow(ESPPage, "ESP ปืนตกพื้น", "ปิด (OFF)", Color3.fromRGB(50, 50, 60), function(btn)
    gunEspEnabled = not gunEspEnabled
    btn.Text = gunEspEnabled and "เปิด (ON)" or "ปิด (OFF)"
    btn.BackgroundColor3 = gunEspEnabled and Color3.fromRGB(0, 162, 255) or Color3.fromRGB(50, 50, 60)
end)

local InfoBox = Instance.new("Frame", ESPPage)
InfoBox.Size = UDim2.new(0.96, 0, 0, 75)
InfoBox.BackgroundColor3 = Color3.fromRGB(26, 26, 32)
Instance.new("UICorner", InfoBox).CornerRadius = UDim.new(0, 6)

local InfoText = Instance.new("TextLabel", InfoBox)
InfoText.Size = UDim2.new(1, -12, 1, 0)
InfoText.Position = UDim2.new(0, 6, 0, 0)
InfoText.Text = "• สีแดง = มีมีด (Knife)\n• สีฟ้า = มีปืน (Gun)\n• สีเขียว = คนปกติ\n• สีเหลือง = ปืนตกพื้น"
InfoText.TextColor3 = Color3.fromRGB(180, 180, 190)
InfoText.Font = Enum.Font.Gotham
InfoText.TextSize = 10
InfoText.TextYAlignment = Enum.TextYAlignment.Center
InfoText.TextXAlignment = Enum.TextXAlignment.Left
InfoText.BackgroundTransparency = 1
table.insert(textLabelData, {Obj = InfoText, BaseSize = 10})

local espTextObjects = {}

local function ScanTools(container)
    local foundKnife, foundGun = false, false
    if container then
        for _, item in pairs(container:GetChildren()) do
            if item:IsA("Tool") then
                local name = string.lower(item.Name)
                if string.find(name, "knife") or string.find(name, "dagger") or string.find(name, "blade") then
                    foundKnife = true
                elseif string.find(name, "gun") or string.find(name, "pistol") or string.find(name, "revolver") then
                    foundGun = true
                end
            end
        end
    end
    return foundKnife, foundGun
end

RunService.RenderStepped:Connect(function()
    for _, textObj in pairs(espTextObjects) do textObj:Remove() end
    espTextObjects = {}

    if espEnabled then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local character = player.Character
                local head = character:FindFirstChild("Head") or character:FindFirstChild("HumanoidRootPart")
                if head then
                    local pos, onScreen = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 1.8, 0))
                    local charKnife, charGun = ScanTools(character)
                    local packKnife, packGun = ScanTools(player:FindFirstChild("Backpack"))

                    local color = Color3.fromRGB(80, 200, 100)
                    if charKnife or packKnife then color = Color3.fromRGB(220, 80, 80)
                    elseif charGun or packGun then color = Color3.fromRGB(0, 162, 255) end

                    local highlight = character:FindFirstChild("ESPHighlight")
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "ESPHighlight"
                        highlight.Parent = character
                    end
                    highlight.FillColor = color
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 0.2

                    if onScreen then
                        local textObj = Drawing.new("Text")
                        textObj.Text = player.Name
                        textObj.Color = color
                        textObj.Position = Vector2.new(pos.X, pos.Y)
                        textObj.Center = true
                        textObj.Outline = true
                        textObj.Size = 12
                        textObj.Visible = true
                        table.insert(espTextObjects, textObj)
                    end
                end
            end
        end
    end
end)

-- **Open / Minimize Animation Logic**
local isOpen = false
local tweenInfo = TweenInfo.new(0.35, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)

local function ToggleUI()
    isOpen = not isOpen
    if isOpen then
        MainFrame.Visible = true
        MainFrame.Size = UDim2.new(0, 44, 0, 44)
        MainFrame.Position = ToggleBtn.Position
        MainFrame.BackgroundTransparency = 1
        
        local targetW = baseWidth * currentScale
        local targetH = baseHeight * currentScale
        
        TweenService:Create(MainFrame, tweenInfo, {
            Size = UDim2.new(0, targetW, 0, targetH),
            Position = UDim2.new(0.5, -targetW/2, 0.5, -targetH/2),
            BackgroundTransparency = 1 - currentAlpha
        }):Play()
    else
        local closeTween = TweenService:Create(MainFrame, tweenInfo, {
            Size = UDim2.new(0, 44, 0, 44),
            Position = ToggleBtn.Position,
            BackgroundTransparency = 1
        })
        closeTween:Play()
        closeTween.Completed:Connect(function()
            if not isOpen then MainFrame.Visible = false end
        end)
    end
end

ToggleBtn.MouseButton1Click:Connect(ToggleUI)
MinBtn.MouseButton1Click:Connect(ToggleUI)

-- **Draggable Window Script**
local Dragging, DragStart, StartPos
TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Dragging = true; DragStart = input.Position; StartPos = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if Dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - DragStart
        MainFrame.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + delta.X, StartPos.Y.Scale, StartPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then Dragging = false end
end)

-- **Loading Sequence & Startup Animation**
task.spawn(function()
    local dots = {"", ".", "..", "..."}
    local startTime = tick()
    
    while tick() - startTime < 2 do
        for _, d in ipairs(dots) do
            LoadText.Text = "กำลังโหลด" .. d
            task.wait(0.3)
        end
    end
    
    LoadText.Text = "ดาวน์โหลดเสร็จแล้ว"
    task.wait(1)
    
    LoadText.Text = "คัดลอกลิงก์ Discord แล้ว"
    pcall(function() setclipboard("https://discord.gg/Ugssja4wqQ") end)
    task.wait(1)
    
    LoadFrame:Destroy()
    
    MainFrame.Visible = true
    MainFrame.Size = UDim2.new(0, 320, 0, 180)
    
    local info = TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    local expandTween = TweenService:Create(MainFrame, info, {
        Size = UDim2.new(0, baseWidth, 0, baseHeight),
        BackgroundTransparency = 0
    })
    expandTween:Play()
end)
