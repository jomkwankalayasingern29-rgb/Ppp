if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")

getgenv().SavedCFrame1 = nil
getgenv().SavedCFrame2 = nil
getgenv().Speed1 = 300
getgenv().Speed2 = 300
getgenv().LoopTeleport1 = false
getgenv().LoopTeleport2 = false

-- เคลียร์ UI เก่าทิ้ง
local CoreGui = game:GetService("CoreGui")
if CoreGui:FindFirstChild("CustomScriptUI") then
    CoreGui.CustomScriptUI:Destroy()
end

-- สร้าง ScreenGui หลัก
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomScriptUI"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- หน้าต่างโหลด
local LoadFrame = Instance.new("Frame")
LoadFrame.Size = UDim2.new(0, 300, 0, 65)
LoadFrame.Position = UDim2.new(0.5, -150, 0.5, -32)
LoadFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LoadFrame.BorderSizePixel = 0
LoadFrame.Parent = ScreenGui

local LoadCorner = Instance.new("UICorner")
LoadCorner.CornerRadius = UDim.new(0, 8)
LoadCorner.Parent = LoadFrame

local LoadStroke = Instance.new("UIStroke")
LoadStroke.Color = Color3.fromRGB(0, 162, 255)
LoadStroke.Thickness = 1.5
LoadStroke.Parent = LoadFrame

local LoadText = Instance.new("TextLabel")
LoadText.Size = UDim2.new(1, 0, 1, 0)
LoadText.BackgroundTransparency = 1
LoadText.Font = Enum.Font.GothamBold
LoadText.Text = "กำลังโหลดระบบ..."
LoadText.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadText.TextSize = 16
LoadText.Parent = LoadFrame

-- หน้าต่าง UI หลัก
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 240)
MainFrame.Position = UDim2.new(0.5, -190, 0.5, -120)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false
MainFrame.BackgroundTransparency = 1
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(0, 162, 255)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- TopBar
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 28)
TopBar.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 8)
TopBarCorner.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -60, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "Meme Sea Mobile UI (Fixed Lag)"
TitleLabel.TextColor3 = Color3.fromRGB(240, 240, 240)
TitleLabel.TextSize = 12
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 24, 0, 24)
CloseButton.Position = UDim2.new(1, -26, 0, 2)
CloseButton.BackgroundTransparency = 1
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(220, 60, 60)
CloseButton.TextSize = 13
CloseButton.Parent = TopBar
CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- ฝั่งซ้าย: หมวดหมู่
local LeftSide = Instance.new("ScrollingFrame")
LeftSide.Size = UDim2.new(0.32, 0, 1, -34)
LeftSide.Position = UDim2.new(0, 5, 0, 30)
LeftSide.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
LeftSide.BorderSizePixel = 0
LeftSide.AutomaticCanvasSize = Enum.AutomaticSize.Y
LeftSide.CanvasSize = UDim2.new(0, 0, 0, 0)
LeftSide.ScrollBarThickness = 2
LeftSide.Parent = MainFrame

local LeftCorner = Instance.new("UICorner")
LeftCorner.CornerRadius = UDim.new(0, 5)
LeftCorner.Parent = LeftSide

local LeftLayout = Instance.new("UIListLayout")
LeftLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
LeftLayout.SortOrder = Enum.SortOrder.LayoutOrder
LeftLayout.Padding = UDim.new(0, 5)
LeftLayout.Parent = LeftSide

-- ฝั่งขวา: ฟังก์ชัน
local RightSide = Instance.new("ScrollingFrame")
RightSide.Size = UDim2.new(0.65, 0, 1, -34)
RightSide.Position = UDim2.new(0.34, 0, 0, 30)
RightSide.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
RightSide.BorderSizePixel = 0
RightSide.AutomaticCanvasSize = Enum.AutomaticSize.Y
RightSide.CanvasSize = UDim2.new(0, 0, 0, 0)
RightSide.ScrollBarThickness = 2
RightSide.Parent = MainFrame

local RightCorner = Instance.new("UICorner")
RightCorner.CornerRadius = UDim.new(0, 5)
RightCorner.Parent = RightSide

local FunctionContainers = {}
local function createContainer(name)
    local container = Instance.new("ScrollingFrame")
    container.Name = name
    container.Size = UDim2.new(1, 0, 1, 0)
    container.BackgroundTransparency = 1
    container.BorderSizePixel = 0
    container.AutomaticCanvasSize = Enum.AutomaticSize.Y
    container.CanvasSize = UDim2.new(0, 0, 0, 0)
    container.ScrollBarThickness = 2
    container.Visible = false
    container.Parent = RightSide

    local layout = Instance.new("UIListLayout")
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)
    layout.Parent = container

    FunctionContainers[name] = container
    return container
end

-- 1. หมวด "เมนูหลัก"
local MainMenuContainer = createContainer("เมนูหลัก")

local function createActionRow(parent, labelText, buttonText, buttonColor, callback)
    local RowFrame = Instance.new("Frame")
    RowFrame.Size = UDim2.new(0.95, 0, 0, 30)
    RowFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    RowFrame.BorderSizePixel = 0
    RowFrame.Parent = parent

    local RowCorner = Instance.new("UICorner")
    RowCorner.CornerRadius = UDim.new(0, 4)
    RowCorner.Parent = RowFrame

    local RowLabel = Instance.new("TextLabel")
    RowLabel.Size = UDim2.new(0.55, 0, 1, 0)
    RowLabel.Position = UDim2.new(0, 8, 0, 0)
    RowLabel.BackgroundTransparency = 1
    RowLabel.Font = Enum.Font.GothamBold
    RowLabel.Text = labelText
    RowLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    RowLabel.TextSize = 11
    RowLabel.TextXAlignment = Enum.TextXAlignment.Left
    RowLabel.Parent = RowFrame

    local ActionBtn = Instance.new("TextButton")
    ActionBtn.Size = UDim2.new(0, 65, 0, 20)
    ActionBtn.Position = UDim2.new(1, -70, 0.5, -10)
    ActionBtn.BackgroundColor3 = buttonColor or Color3.fromRGB(50, 100, 200)
    ActionBtn.BorderSizePixel = 0
    ActionBtn.Font = Enum.Font.GothamBold
    ActionBtn.Text = buttonText
    ActionBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    ActionBtn.TextSize = 10
    ActionBtn.Parent = RowFrame

    local ActionCorner = Instance.new("UICorner")
    ActionCorner.CornerRadius = UDim.new(0, 3)
    ActionCorner.Parent = ActionBtn

    ActionBtn.MouseButton1Click:Connect(function()
        callback(ActionBtn)
    end)

    return RowFrame
end

local function createInputRow(parent, labelText, defaultVal, callback)
    local RowFrame = Instance.new("Frame")
    RowFrame.Size = UDim2.new(0.95, 0, 0, 30)
    RowFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    RowFrame.BorderSizePixel = 0
    RowFrame.Parent = parent

    local RowCorner = Instance.new("UICorner")
    RowCorner.CornerRadius = UDim.new(0, 4)
    RowCorner.Parent = RowFrame

    local RowLabel = Instance.new("TextLabel")
    RowLabel.Size = UDim2.new(0.55, 0, 1, 0)
    RowLabel.Position = UDim2.new(0, 8, 0, 0)
    RowLabel.BackgroundTransparency = 1
    RowLabel.Font = Enum.Font.GothamBold
    RowLabel.Text = labelText
    RowLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    RowLabel.TextSize = 11
    RowLabel.TextXAlignment = Enum.TextXAlignment.Left
    RowLabel.Parent = RowFrame

    local TextBox = Instance.new("TextBox")
    TextBox.Size = UDim2.new(0, 65, 0, 20)
    TextBox.Position = UDim2.new(1, -70, 0.5, -10)
    TextBox.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    TextBox.BorderSizePixel = 0
    TextBox.Font = Enum.Font.GothamBold
    TextBox.Text = tostring(defaultVal)
    TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextBox.TextSize = 10
    TextBox.Parent = RowFrame

    local TextCorner = Instance.new("UICorner")
    TextCorner.CornerRadius = UDim.new(0, 3)
    TextCorner.Parent = TextBox

    TextBox.FocusLost:Connect(function()
        local num = tonumber(TextBox.Text)
        if num then
            callback(num)
        else
            TextBox.Text = tostring(defaultVal)
        end
    end)

    return RowFrame
end

createActionRow(MainMenuContainer, "ขายทั้งหมด", "ขาย", Color3.fromRGB(50, 100, 200), function(btn)
    pcall(function()
        game:GetService("ReplicatedStorage"):WaitForChild("RequestSell"):InvokeServer("sellAll")
    end)
    btn.Text = "สำเร็จ!"
    task.wait(1)
    btn.Text = "ขาย"
end)

-- 2. หมวด "ระบบวาร์ป"
local TeleportContainer = createContainer("ระบบวาร์ป")

local function smoothTeleport(targetCFrame, speed)
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
    local hrp = LocalPlayer.Character.HumanoidRootPart
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local s = speed or 300
    if s <= 0 then s = 300 end
    local timeTake = distance / s
    
    local tweenInfo = TweenInfo.new(timeTake, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
    tween:Play()
    task.wait(timeTake)
end

-- จุดที่ 1
createActionRow(TeleportContainer, "จุดที่ 1", "เซฟจุด", Color3.fromRGB(80, 80, 80), function(btn)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        getgenv().SavedCFrame1 = LocalPlayer.Character.HumanoidRootPart.CFrame
        btn.Text = "เซฟแล้ว!"
        task.wait(1)
        btn.Text = "เซฟจุด"
    end
end)

createInputRow(TeleportContainer, "ความเร็ว จุด 1", getgenv().Speed1, function(val)
    getgenv().Speed1 = val
end)

createActionRow(TeleportContainer, "วาร์ปไปจุด 1", "วาร์ป", Color3.fromRGB(50, 150, 50), function(btn)
    if getgenv().SavedCFrame1 then
        smoothTeleport(getgenv().SavedCFrame1, getgenv().Speed1)
        btn.Text = "ถึงแล้ว!"
        task.wait(1)
        btn.Text = "วาร์ป"
    end
end)

createActionRow(TeleportContainer, "ลูปวาร์ป จุด 1", "OFF", Color3.fromRGB(200, 50, 50), function(btn)
    getgenv().LoopTeleport1 = not getgenv().LoopTeleport1
    if getgenv().LoopTeleport1 then
        btn.Text = "ON"
        btn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        task.spawn(function()
            while getgenv().LoopTeleport1 do
                if getgenv().SavedCFrame1 then
                    smoothTeleport(getgenv().SavedCFrame1, getgenv().Speed1)
                end
                task.wait(0.5) -- เพิ่มเวลาหน่วงเพื่อป้องกันเกมแล็กสะสม
            end
        end)
    else
        btn.Text = "OFF"
        btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    end
end)

-- จุดที่ 2
createActionRow(TeleportContainer, "จุดที่ 2", "เซฟจุด", Color3.fromRGB(80, 80, 80), function(btn)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        getgenv().SavedCFrame2 = LocalPlayer.Character.HumanoidRootPart.CFrame
        btn.Text = "เซฟแล้ว!"
        task.wait(1)
        btn.Text = "เซฟจุด"
    end
end)

createInputRow(TeleportContainer, "ความเร็ว จุด 2", getgenv().Speed2, function(val)
    getgenv().Speed2 = val
end)

createActionRow(TeleportContainer, "วาร์ปไปจุด 2", "วาร์ป", Color3.fromRGB(50, 150, 50), function(btn)
    if getgenv().SavedCFrame2 then
        smoothTeleport(getgenv().SavedCFrame2, getgenv().Speed2)
        btn.Text = "ถึงแล้ว!"
        task.wait(1)
        btn.Text = "วาร์ป"
    end
end)

createActionRow(TeleportContainer, "ลูปวาร์ป จุด 2", "OFF", Color3.fromRGB(200, 50, 50), function(btn)
    getgenv().LoopTeleport2 = not getgenv().LoopTeleport2
    if getgenv().LoopTeleport2 then
        btn.Text = "ON"
        btn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        task.spawn(function()
            while getgenv().LoopTeleport2 do
                if getgenv().SavedCFrame2 then
                    smoothTeleport(getgenv().SavedCFrame2, getgenv().Speed2)
                end
                task.wait(0.5) -- เพิ่มเวลาหน่วงเพื่อป้องกันเกมแล็กสะสม
            end
        end)
    else
        btn.Text = "OFF"
        btn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    end
end)

local function switchTab(tabName)
    for name, container in pairs(FunctionContainers) do
        container.Visible = (name == tabName)
    end
end

local function createCategoryButton(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.9, 0, 0, 30)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.BorderSizePixel = 0
    btn.Font = Enum.Font.GothamBold
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Parent = LeftSide

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 4)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        switchTab(name)
    end)
end

createCategoryButton("เมนูหลัก")
createCategoryButton("ระบบวาร์ป")

task.spawn(function()
    task.wait(1.5)
    LoadFrame:Destroy()
    MainFrame.Visible = true
    MainFrame.Size = UDim2.new(0, 320, 0, 180)
    switchTab("เมนูหลัก")
    
    local info = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
    TweenService:Create(MainFrame, info, {
        Size = UDim2.new(0, 380, 0, 240),
        BackgroundTransparency = 0
    }):Play()
end)
